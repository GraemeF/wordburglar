define('lib/Game', function (Game) {

  return describe('Game', function () {
    var test;

    beforeEach(function () {
      test = new Game();
    });

    it('should!', function () {});
  });
});